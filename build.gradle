plugins {
id "org.sonarqube" version "2.6.2" apply false
}

subprojects { subproject ->
  apply plugin: 'org.sonarqube'
  apply plugin: 'jacoco'
  apply plugin: 'java'


  repositories {
    jcenter()
  }
}
