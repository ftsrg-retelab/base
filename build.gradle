subprojects {
  apply plugin: 'java'

  repositories {
    jcenter()
  }
  dependencies {
    
    testImplementation 'junit:junit:4.12'
    implementations group: 'com.google.guava', name: 'guava', version: '30.1-jre'
}
}
